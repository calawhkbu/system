{
  "name": "system",
  "description": "This the base custom config",
  "dependencies": {
    "moment": "^2.24.0",
    "node-jql": "^2.0.0-rc.51",
    "node-jql-core": "^v2.0.0-rc.40"
  },
  "devDependencies": {
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "lint": "tslint -p tsconfig.json -c tslint.json",
    "lint:fix": "tslint -p tsconfig.json -c tslint.json --fix"
  }
}